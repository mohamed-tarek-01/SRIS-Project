; PHP settings for Laravel's built-in server (php artisan serve)
; These override the CLI defaults for the built-in server process.
post_max_size=500M
upload_max_filesize=500M
memory_limit=512M
max_execution_time=300
